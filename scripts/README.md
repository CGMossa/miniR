# scripts

Utility scripts for development and testing.

- `parse-test.sh` — test the parser against a directory of .R files, reporting pass/fail/crash rates
- `fix-vendor-checksum.sh` — recalculate `.cargo-checksum.json` for a vendored crate after patching
