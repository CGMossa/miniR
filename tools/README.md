# tools

Build and configuration tools from the GNU R source tree. Contains autoconf helpers, install scripts, and other build infrastructure.

These are upstream R build tools, not used by miniR's Rust build system.
